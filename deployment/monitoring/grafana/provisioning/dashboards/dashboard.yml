apiVersion: 1

providers:
  - name: 'iris-pgwire-dashboards'
    orgId: 1
    folder: 'IRIS PostgreSQL Wire Protocol'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards